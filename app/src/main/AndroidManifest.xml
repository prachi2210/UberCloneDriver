<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wizebrain.adebdriver">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ADEBDriver">
        <activity android:name=".activity.PaymentSuccessActivity">

        </activity>
        <activity android:name=".activity.RateUserActivity" />
        <activity android:name=".activity.NotificationActivity" />
        <activity android:name=".activity.SubmissionActivity" />
        <activity android:name=".activity.HealthReportActivity" />
        <activity android:name=".activity.PersonalIdActivity" />
        <activity android:name=".activity.DocumentActivity" />
        <activity android:name=".activity.DrivingLicenseActivity" />
        <activity android:name=".activity.VerifyOtpActivity" />
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.Splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>